pipeline {
    agent any
    environment {
        DOCKERHUB_USERNAME = "prvinsm21"
    }
    stages {
        stage ('Git checkout') {
            steps {
                sh 'echo Passed'
            }
        }
    }
}